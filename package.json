{
  "name": "tengoku",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "gate": "npm run typecheck && npm run test && npm run build"
  },
  "dependencies": {
    "wasmoon": "1.16.0"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "vite": "^7.1.0",
    "vitest": "^5.0.0"
  }
}
